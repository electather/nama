# Resources

- Keep user-facing text in `Localizable.xcstrings`; add translator comments
  where context is ambiguous.
- Add entitlements and privacy descriptions with the behavior that requires
  them.
- Prefer system assets. Record licensing and verify contrast and scale before
  adding a custom asset.
