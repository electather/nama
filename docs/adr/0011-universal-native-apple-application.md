# Build one universal native Apple application

Nama's first Apple client is one Swift/SwiftUI application rooted at `apps/ios` for iOS, tvOS, and macOS, sharing product behavior and adding platform adapters only where platform APIs differ. Shared behavior and one product outweigh separately optimized platform codebases.
