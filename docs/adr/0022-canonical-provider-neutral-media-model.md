# Store a canonical provider-neutral item/source model

Nama stores canonical items with one or more sources and maps opaque provider observations into that model; public reads neither proxy provider models nor depend on retained provider payloads. This accepts reconciliation complexity over one-to-one provider mirrors so integrations remain replaceable and people see one coherent library.
