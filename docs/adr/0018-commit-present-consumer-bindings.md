# Commit generated bindings only for present consumers

Nama commits generated bindings and drift-checks them so real consumers compile against one reviewed contract revision. Generation remains consumer-driven: it creates bindings only for products that exist, accepting committed output and regeneration discipline rather than on-demand or speculative clients.
