# Require Apple platform version 26

Nama's first universal Apple application requires iOS 26, tvOS 26, and macOS 26 or later. This replaces the earlier iOS 17, tvOS 17, and macOS 14 targets: before any application ships, one current platform baseline is worth excluding older devices to avoid immediate compatibility branches across three new surfaces.
